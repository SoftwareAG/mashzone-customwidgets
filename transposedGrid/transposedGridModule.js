/**
 * Module for the widget
 * Every code that is part of the transposedGrid should be inside the transposedGrid module.
 */
angular.module('transposedGridModule', ['prestoDashboard.dashboardProvider']);